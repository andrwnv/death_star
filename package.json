{
  "name": "acme-api",
  "version": "1.0.0",
  "dependencies": {
    "@redocly/cli": "1.0.0-beta.127"
  },
  "private": true,
  "scripts": {
    "start": "redocly preview-docs",
    "build": "redocly bundle openapi/openapi.yaml -o dist.json",
    "join": "redocly join openapi/paths/*.yaml --prefix-tags-with-filename true --output=openapi/openapi.yaml",
    "test": "redocly lint"
  }
}
